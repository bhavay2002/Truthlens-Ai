diff --git a/src/models/registry/model_factory.py b/src/models/registry/model_factory.py
--- a/src/models/registry/model_factory.py
+++ b/src/models/registry/model_factory.py
@@
-        merged_config = dict(config)
+        import copy
+        merged_config = copy.deepcopy(config)


diff --git a/src/models/tasks/narrative/narrative_detector.py b/src/models/tasks/narrative/narrative_detector.py
--- a/src/models/tasks/narrative/narrative_detector.py
+++ b/src/models/tasks/narrative/narrative_detector.py
@@
-        original_threshold = self.classifier_head.config.threshold
-        if threshold is not None:
-            self.classifier_head.config.threshold = float(threshold)
+        # thread-safe: avoid mutating config

@@
-        if threshold is not None:
-            self.classifier_head.config.threshold = original_threshold
+        if threshold is not None:
+            outputs["probabilities"] = (
+                outputs["probabilities"] > float(threshold)
+            ).float()


diff --git a/src/models/utils/model_utils.py b/src/models/utils/model_utils.py
--- a/src/models/utils/model_utils.py
+++ b/src/models/utils/model_utils.py
@@
     if not isinstance(text, str):
         raise TypeError("Input text must be a string")
+    if not text.strip():
+        raise ValueError("Input text cannot be empty")
diff --git a/src/models/registry/model_factory.py b/src/models/registry/model_factory.py
index 2222222..3333333 100644
--- a/src/models/registry/model_factory.py
+++ b/src/models/registry/model_factory.py
@@ -136,7 +136,9 @@ class ModelFactory:
         if not isinstance(config, dict):
             raise TypeError("config must be a dictionary")
-        merged_config = dict(config)
+        import copy
+        merged_config = copy.deepcopy(config)
+
         merged_config.update(ModelFactory._resolve_encoder_fields(config))
         merged_config.update(ModelFactory._resolve_regression_fields(config))


diff --git a/src/models/registry/model_registry.py b/src/models/registry/model_registry.py
index 4444444..5555555 100644
--- a/src/models/registry/model_registry.py
+++ b/src/models/registry/model_registry.py
@@ -139,12 +139,14 @@ class ModelRegistry:
-                elif not MODEL_DIR.exists():
-                    raise FileNotFoundError(f"Model path not found: {MODEL_DIR}")
+                else:
+                    raise FileNotFoundError(f"Model path not found: {named_model_path}")

@@ -216,7 +218,15 @@ class ModelRegistry:
-        model.load_state_dict(state_dict)
+        missing, unexpected = model.load_state_dict(state_dict, strict=False)
+
+        if missing:
+            logger.warning("Missing keys: %s", missing)
+        if unexpected:
+            logger.warning("Unexpected keys: %s", unexpected)


diff --git a/src/models/tasks/narrative/narrative_detector.py b/src/models/tasks/narrative/narrative_detector.py
index 8888888..9999999 100644
--- a/src/models/tasks/narrative/narrative_detector.py
+++ b/src/models/tasks/narrative/narrative_detector.py
@@ -183,11 +183,12 @@ class NarrativeDetector(BaseModel):
-        original_threshold = self.classifier_head.config.threshold
-        if threshold is not None:
-            self.classifier_head.config.threshold = float(threshold)

         outputs = self.forward(
             input_ids=input_ids,
             attention_mask=attention_mask,
         )

-        if threshold is not None:
-            self.classifier_head.config.threshold = original_threshold
+        if threshold is not None:
+            outputs["probabilities"] = (
+                outputs["probabilities"] > float(threshold)
+            ).float()


diff --git a/src/models/tasks/bias/bias_classifier.py b/src/models/tasks/bias/bias_classifier.py
index aaaaaaa..bbbbbbb 100644
--- a/src/models/tasks/bias/bias_classifier.py
+++ b/src/models/tasks/bias/bias_classifier.py
@@ -192,7 +192,9 @@ class BiasClassifier(BaseModel):
-            if labels.min().item() < 0 or labels.max().item() >= self.NUM_CLASSES:
+            if not ((labels >= 0).all() and (labels < self.NUM_CLASSES).all()):
                 raise ValueError(
                     f"labels must be in range [0, {self.NUM_CLASSES - 1}]"
                 )


diff --git a/src/models/tasks/propaganda/propaganda_detector.py b/src/models/tasks/propaganda/propaganda_detector.py
index ccccccc..ddddddd 100644
--- a/src/models/tasks/propaganda/propaganda_detector.py
+++ b/src/models/tasks/propaganda/propaganda_detector.py
@@ -211,7 +211,9 @@ class PropagandaDetector(BaseModel):
-            if labels.min().item() < 0 or labels.max().item() >= self.NUM_CLASSES:
+            if not ((labels >= 0).all() and (labels < self.NUM_CLASSES).all()):
                 raise ValueError(
                     f"labels must be in range [0, {self.NUM_CLASSES - 1}]"
                 )


diff --git a/src/models/tasks/ideology/ideology_classifier.py b/src/models/tasks/ideology/ideology_classifier.py
index eeeeeee..ffffff0 100644
--- a/src/models/tasks/ideology/ideology_classifier.py
+++ b/src/models/tasks/ideology/ideology_classifier.py
@@ -224,7 +224,9 @@ class IdeologyClassifier(BaseModel):
-            if labels.min().item() < 0 or labels.max().item() >= self.NUM_CLASSES:
+            if not ((labels >= 0).all() and (labels < self.NUM_CLASSES).all()):
                 raise ValueError(
                     f"labels must be in range [0, {self.NUM_CLASSES - 1}]"
                 )


diff --git a/src/models/training/loss_functions.py b/src/models/training/loss_functions.py
index 1212121..2323232 100644
--- a/src/models/training/loss_functions.py
+++ b/src/models/training/loss_functions.py
@@ -71,6 +71,10 @@ class LossFactory:
         if not isinstance(config.weight, (int, float)):
             raise TypeError("weight must be numeric")

+        weight = float(config.weight)
+
         if config.loss_type == "binary":
-            base = nn.BCEWithLogitsLoss(reduction=config.reduction)
+            base = nn.BCEWithLogitsLoss(reduction=config.reduction)
+            return lambda logits, targets: base(logits, targets) * weight


@@ -148,6 +152,11 @@ def binary_classification_loss(
-    if __debug__:
-        if targets.dim() == 1:
-            targets = targets.unsqueeze(1)
-        if logits.shape != targets.shape:
-            raise RuntimeError(...)
+    if targets.dim() == 1:
+        targets = targets.unsqueeze(1)
+
+    if logits.shape != targets.shape:
+        raise RuntimeError(
+            f"Binary loss shape mismatch: logits {logits.shape} vs targets {targets.shape}"
+        )


diff --git a/src/models/training/trainer.py b/src/models/training/trainer.py
index 1414141..2525252 100644
--- a/src/models/training/trainer.py
+++ b/src/models/training/trainer.py
@@ -231,7 +231,11 @@ class Trainer:
-            if (
-                self.checkpoint_manager is not None
-                and self.config.checkpoint_every_steps > 0
-                and self.global_step % self.config.checkpoint_every_steps == 0
-            ):
+            if (
+                self.checkpoint_manager is not None
+                and self.config.checkpoint_every_steps > 0
+                and self.global_step > 0
+                and self.global_step % self.config.checkpoint_every_steps == 0
+            ):


diff --git a/src/models/training/training_step.py b/src/models/training/training_step.py
index 1616161..2727272 100644
--- a/src/models/training/training_step.py
+++ b/src/models/training/training_step.py
@@ -118,7 +118,7 @@ class TrainingStep:
-        with torch.autocast(
-            device_type="cuda" if self.use_amp else "cpu",
+        with torch.autocast(
+            device_type="cuda",
             dtype=self.autocast_dtype,
             enabled=self.use_amp,
         ):
@@ -126,6 +126,10 @@ class TrainingStep:

+        if (step % self.config.gradient_accumulation_steps) == 0:
+            self.optimizer.zero_grad(set_to_none=True)
+

diff --git a/src/models/utils/weight_initialization.py b/src/models/utils/weight_initialization.py
index 2020202..3030303 100644
--- a/src/models/utils/weight_initialization.py
+++ b/src/models/utils/weight_initialization.py
@@ -70,6 +70,9 @@ def initialize_weights(
             elif method == "uniform":
                 nn.init.uniform_(module.weight, a=-0.1, b=0.1)
             else:
                 raise ValueError(f"Unsupported initialization method: {method}")

+            if module.bias is not None:
+                nn.init.constant_(module.bias, 0.0)


diff --git a/src/models/utils/model_utils.py b/src/models/utils/model_utils.py
index 2222222..3333333 100644
--- a/src/models/utils/model_utils.py
+++ b/src/models/utils/model_utils.py
@@ -118,6 +118,10 @@ def preprocess_text(text: str) -> str:
     if text is None:
         raise ValueError("Input text cannot be None")
     if not isinstance(text, str):
         raise TypeError("Input text must be a string")
+    if not text.strip():
+        raise ValueError("Input text cannot be empty")

     normalized = text.replace("\n", " ").replace("\t", " ")