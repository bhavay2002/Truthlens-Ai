You are a senior ML systems architect and codebase auditor.

Your task is to perform a FULL REPOSITORY STRUCTURAL AUDIT and CLEANUP for a machine learning system (TruthLens AI).

---

## 🎯 OBJECTIVE

Detect, analyze, and fix ALL duplicate, conflicting, or redundant systems across the repository.

This includes (but is NOT limited to):

* models/
* src/models/
* features/
* src/features/
* pipelines/
* src/pipelines/
* training/
* src/training/
* inference paths
* checkpoint systems
* utilities duplication

---

## ⚠️ CRITICAL RULES (STRICT)

1. SINGLE SOURCE OF TRUTH:

   * ALL core logic MUST live inside:
     src/
   * Root-level folders must NOT contain logic

2. ROOT FOLDER POLICY:

   * models/ → ONLY artifacts (weights, tokenizer, vectorizers)
   * data/ → datasets
   * logs/ → logs
   * reports/ → outputs

3. ZERO DUPLICATION:
   If duplicate modules exist:

   * KEEP the version inside src/
   * REMOVE or deprecate the root-level version

4. IMPORT CONSISTENCY:
   Replace ALL:
   from models.xxx
   from pipelines.xxx
   from features.xxx

   WITH:
   from src.models.xxx
   from src.pipelines.xxx
   from src.features.xxx

---

## 🔍 TASKS TO PERFORM

STEP 1 — DETECT DUPLICATES

* Scan entire repo for duplicate folders:
  models vs src/models
  pipelines vs src/pipelines
  features vs src/features
  training vs src/training
* List ALL conflicts

STEP 2 — CLASSIFY EACH DUPLICATE
For each duplicate:

* Which file is actually used?
* Which is outdated?
* Which is referenced in imports?

STEP 3 — FIX STRUCTURE

* Move ALL logic into src/
* Remove or mark deprecated:
  models/inference/
  models/checkpointing/
  models/utils/
  pipelines/
  features/

STEP 4 — FIX IMPORTS (CRITICAL)

* Rewrite ALL imports to use src.*
* Ensure no broken imports remain

STEP 5 — CLEAN MODEL SYSTEM

* Ensure:
  src/models/ = ONLY architecture
  models/ = ONLY artifacts

STEP 6 — STANDARDIZE INFERENCE
Ensure ONLY:
src/inference/
model_loader.py
inference_engine.py
prediction_pipeline.py

Remove any alternative inference paths.

STEP 7 — CLEAN DATA PIPELINE

* Remove duplicate preprocessing files
* Keep only one clean_data.py
* Ensure train and inference use same pipeline

STEP 8 — VALIDATE RUNTIME STRUCTURE
Ensure these folders exist (auto-create if missing):
checkpoints/
outputs/
saved_models/

STEP 9 — VERIFY CONSISTENCY

* No duplicate modules remain
* No conflicting imports
* No unused files

STEP 10 — TEST IMPACT
List:

* Files modified
* Files deleted
* Imports fixed
* Any potential breaking changes

---

## 📦 OUTPUT FORMAT

1. DUPLICATE REPORT

   * List all duplicated modules

2. ACTION PLAN

   * What will be removed / kept

3. PATCH (CODE CHANGES)

   * Show exact import fixes
   * Show file deletions

4. FINAL STRUCTURE

   * Clean folder tree

5. WARNINGS

   * Any risky changes

---

## 🔥 EXTRA REQUIREMENTS

* Do NOT silently ignore duplicates
* Do NOT keep both versions
* Do NOT allow ambiguous imports
* Prefer deterministic, production-safe structure

---

## END OF TASK
