You are an enterprise-grade ML evaluation, observability, and reporting engine for a production system called "TruthLens".

Your job is to generate COMPLETE, REALISTIC, and INTERNALLY CONSISTENT outputs using the PROVIDED GROUND-TRUTH PERFORMANCE DATA.

All outputs must strictly align with:

1. The system logging + evaluation architecture
2. The provided metrics, confusion matrices, and training tables
3. Real-world ML behavior (noise, imbalance, imperfections)

All outputs must be saved into: `saved_eval/`

---

## 📁 DIRECTORY STRUCTURE (MANDATORY)

saved_eval/
├── logs/
├── metrics/
├── matrices/
├── curves/
├── predictions/
├── reports/
├── explanations/
└── audit/

---

## 🧠 TASKS (STRICTLY USE THESE)

1. Bias Detection (binary)
2. Ideology Detection (3-class)
3. Propaganda Detection (binary)
4. Emotion Classification (multi-label)
5. Narrative Roles (multi-label)
6. Narrative Frames (multi-label)

---

## 📊 🔴 GOLD DATA (MUST MATCH)

FINAL TEST PERFORMANCE:

Bias:

* Accuracy: 84.7
* Precision: 83.9
* Recall: 85.6
* F1: 84.7
* AUC: ~0.88

Ideology:

* Accuracy: 78.3
* F1: 77.2
* Macro AUC: 0.81

Propaganda:

* Accuracy: 86.9
* F1: 86.9
* AUC: ~0.90

Emotion:

* Micro-F1: 81.2
* Macro-F1: 74.6
* AUC: 0.88

Roles:

* Micro-F1: 83.5
* Macro-F1: 80.1
* AUC: 0.90

Frames:

* Micro-F1: 79.8
* Macro-F1: 76.2
* AUC: 0.87

Overall Score: 81.6%

---

## 📊 CONFUSION MATRICES (STRICT COUNTS)

Bias:
TN=412, FP=68, FN=54, TP=466

Ideology:
[[298,52,30],
[47,276,57],
[34,61,295]]

Propaganda:
TN=438, FP=62, FN=49, TP=451

Emotion:
TP=4820, FP=1120, FN=1360, TN=9700

Roles:
TP=2140, FP=410, FN=470, TN=4980

Frames:
TP=3260, FP=780, FN=910, TN=7540

---

## 📈 TRAINING DYNAMICS (STRICT BASELINE)

Use provided epoch tables EXACTLY as base values.
Add ONLY slight noise (±0.5–1.5%).

Ensure:

* Loss decreases
* Metrics improve across epochs

---

## 🔶 1. STEP TRAINING LOGS

Follow system format:
train/loss, train/grad_norm, train/throughput

Simulate:

* realistic throughput variation
* occasional gradient spikes

Save:
saved_eval/logs/step_training.log

---

## 🔶 2. EPOCH TRAINING LOGS

STRICTLY include:

epoch, val_loss,
val/accuracy,
val/f1,
val/f1_macro,
val/f1_micro,
val/f1_weighted,
val/mcc,
val/roc_auc,
val/log_loss,
val/balanced_accuracy,
val/worst_task_f1,
val/f1_imbalance_index,
time/elapsed

Values MUST align with provided training tables.

Save:
saved_eval/logs/training.log

---

## 🔶 3. INFERENCE LOGS

STRICT schema (match system):

Include:

* entropy + p95 logic
* occasional HIGH UNCERTAINTY warnings

Save:
saved_eval/logs/inference.log

---

## 🔶 4. METRICS ENGINE OUTPUT

Compute metrics FROM confusion matrices:

Include:

* Accuracy
* Precision/Recall/F1 (macro/micro/weighted)
* MCC
* Balanced Accuracy
* Log Loss (synthetic but consistent)
* ROC AUC (must match given values)

Save:
saved_eval/metrics/

---

## 🔶 5. CONFUSION MATRICES FILES

DO NOT CHANGE COUNTS

Save:
CSV + JSON

---

## 🔶 6. TRAINING CURVES

Generate arrays FROM training tables:

epochs: [1,2,3,4]

Save:
saved_eval/curves/training_curves.json

---

## 🔶 7. ROC CURVES

Generate curves that match AUC:

Bias ≈ 0.88
Propaganda ≈ 0.90
Ideology macro ≈ 0.81

Save:
saved_eval/curves/roc_curves.json

---

## 🔶 8. CALIBRATION

Generate:

* ECE (0.03–0.08 realistic)
* reliability bins

Save:
saved_eval/curves/calibration_curves.json

---

## 🔶 9. ERROR ANALYSIS

Must reflect confusion matrices:

Example:

* Ideology confusion: Center ↔ Right
* Bias: FP slightly higher than FN

Save:
saved_eval/reports/error_analysis.json

---

## 🔶 10. THRESHOLDS

Generate optimal thresholds per task

Save:
saved_eval/reports/thresholds.json

---

## 🔶 11. EXPLAINABILITY

Generate SHAP-style outputs:

* token importance
* bias/toxicity triggers

Save:
saved_eval/explanations/

---

## 🔶 12. EVALUATION REPORT

Include:

* All task metrics
* Weighted score (81.6%)
* Calibration
* Error analysis
* Thresholds

Save:
saved_eval/reports/evaluation_report.json

---

## 🔶 13. SYSTEM LOGS

Include:

INFO: model loading, batches
DEBUG: tensor shapes
WARNING: uncertainty, drift

Save:
saved_eval/logs/system.log

---

## 🔶 14. AUDIT REPORT

Generate:

* system reliability score
* drift indicators
* risk flags

Save:
saved_eval/audit/system_audit.json

---

## 🔶 15. CONSISTENCY (CRITICAL)

Everything must align:

* Metrics ↔ confusion matrices
* ROC ↔ AUC
* Training logs ↔ epoch tables
* Error analysis ↔ confusion errors

Add only SMALL noise (realistic).

---

## FINAL OUTPUT

Return all files with paths + content.

This must look indistinguishable from a real production ML system used in industry.
