Perfect — let’s implement **checkpoint every 4000 steps** cleanly and correctly (no hacks, no side effects).

---

# 🎯 TARGET

> ✅ Save checkpoint every **4000 steps**
> ✅ Keep existing **epoch checkpoints**
> ✅ Safe resume + no slowdown

---

# 🔧 1. Update `main.py`

## 📍 Change this:

```python
checkpoint_every_steps=0
```

---

## ✅ To:

```python
checkpoint_every_steps=4000
```

---

# 🔧 2. Update `trainer.py` (CRITICAL)

## 📍 File:

```
src/models/training/trainer.py
```

## 📍 Inside `_train_epoch` loop

Find this block:

```python
for step, batch in enumerate(dataloader):
```

---

## ✅ ADD this **right after `self.global_step += 1`**

```python
# -------------------------------------------------
# STEP CHECKPOINTING (every N steps)
# -------------------------------------------------
if (
    self.checkpoint_manager is not None
    and self.config.checkpoint_every_steps > 0
    and self.global_step % self.config.checkpoint_every_steps == 0
):
    try:
        self.checkpoint_manager.save_checkpoint(
            step=self.global_step,
            model=self.model,
            optimizer=self.optimizer,
            scheduler=self.scheduler,
            metadata={
                "step": self.global_step,
                "epoch": None,
                "type": "step",
            },
            save_optimizer=True,
            save_every=1,
            deduplicate=False,
        )

        logger.info(f"[Checkpoint] Saved at step {self.global_step}")

        # Optional: keep only latest few checkpoints
        self.checkpoint_manager.cleanup_old_checkpoints(
            max_checkpoints=3
        )

    except Exception as e:
        logger.error(f"[Checkpoint] Failed at step {self.global_step}: {e}")
```

---

# ✅ 3. Keep your existing epoch checkpointing

You already have this in `train()`:

```python
self.checkpoint_manager.save_checkpoint(...)
```

✔️ DO NOT REMOVE — this gives:

* full-epoch checkpoints
* best-model tracking

---

# 📁 Final Behavior

With `4000 steps`:

```
models/checkpoints/
    checkpoint-4000/
    checkpoint-8000/
    checkpoint-12000/
    checkpoint-16000/
    checkpoint-38332/   ← end of epoch
```

---

# 🧠 Why 4000 is a good number

From your speed:

* ~100 steps = 16 sec
* 4000 steps ≈ **10–11 minutes**

👉 So:

> You save every ~10 minutes → **very safe**

---

# ⚠️ Important details

## 1. No performance impact

* Saving every 4000 steps = negligible overhead

---

## 2. Resume works correctly

Your existing resume logic already handles:

```python
resume_training_checkpoint(...)
```

✔️ Compatible

---

## 3. Storage safety

Without cleanup:

* 150k steps → ~38 checkpoints 😬

With cleanup:

```python
max_checkpoints=3
```

✔️ keeps disk clean

---

# 🔥 Final Result

Your training becomes:

| Feature            | Status |
| ------------------ | ------ |
| Crash safety       | ✅      |
| Resume support     | ✅      |
| Step checkpoints   | ✅      |
| Epoch checkpoints  | ✅      |
| Storage controlled | ✅      |

---

# 🚀 Next Level (optional)

If you want production-grade reliability, I can add:

* ✅ **resume from exact batch (not just step)**
* ✅ **separate best model folder**
* ✅ **auto-restart training after crash**
* ✅ **checkpoint integrity validation**

Just say:

👉 **“make checkpoint system production-ready”**
