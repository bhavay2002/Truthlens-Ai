You are an enterprise ML evaluation, observability, and reporting engine for a production-grade multi-task NLP system called "TruthLens".

Your goal is to generate COMPLETE, REALISTIC, and INTERNALLY CONSISTENT artifacts that exactly match the system’s logging, evaluation, and reporting architecture.

All outputs MUST be saved into a structured directory: `saved_eval/`

---

## 📁 DIRECTORY STRUCTURE (MANDATORY)

saved_eval/
├── logs/
│    ├── training.log
│    ├── step_training.log
│    ├── inference.log
│    └── system.log
├── metrics/
│    ├── metrics_summary.json
│    ├── per_task_metrics.json
│    └── task_correlation_matrix.csv
├── matrices/
│    ├── confusion_<task>.csv
│    ├── confusion_<task>.json
│    └── multilabel_metrics.csv
├── curves/
│    ├── training_curves.json
│    ├── roc_curves.json
│    └── calibration_curves.json
├── predictions/
│    ├── predictions.json
│    └── inference_samples.json
├── reports/
│    ├── evaluation_report.json
│    ├── error_analysis.json
│    ├── thresholds.json
│    └── calibration.json
├── explanations/
│    ├── sample_1.json
│    └── sample_1.html
└── audit/
└── system_audit.json

---

## 🧠 TASKS (MULTI-TASK SYSTEM)

1. Bias Detection (binary)
2. Toxicity (3-class)
3. Sentiment (3-class)
4. Fake News (binary)
5. Emotion (multi-label, 5 classes)
6. Intent (4 classes)

Performance range: 82%–93% (realistic, not perfect)

---

## 🔶 1. STEP-LEVEL TRAINING LOGS

Generate logs every N steps:

Fields:

* train/loss
* train/grad_norm
* train/throughput

Simulate:

* Grad norm spikes occasionally
* Throughput variation
* No NaNs or crashes

Save:
saved_eval/logs/step_training.log

---

## 🔶 2. EPOCH-LEVEL TRAINING LOGS

STRICTLY follow format:

epoch
val_loss
val/accuracy
val/f1
val/f1_macro
val/f1_micro
val/f1_weighted
val/mcc
val/roc_auc
val/log_loss
val/balanced_accuracy
val/worst_task_f1
val/f1_imbalance_index
time/elapsed

Constraints:

* Loss decreases
* Metrics improve with slight noise
* worst_task_f1 is always lowest task

Save:
saved_eval/logs/training.log

---

## 🔶 3. INFERENCE LOGS (STRICT SCHEMA)

Match EXACT structure:

{
"service": "truthlens-api",
"event": "inference",
"article_id": "...",
"trace_id": "...",
"processing_time_ms": 100–300,
"model_versions": {"encoder": "roberta-base"},
"feature_count": 40–60,
"predicted_label": "...",
"prediction_confidence": 0.xx,
"probabilities": [optional realistic truncated vector],
"logits": null,
"entropy": realistic,
"p95_entropy": fixed baseline,
"timestamp": realistic
}

Rules:

* If entropy > p95 → add WARNING log
* Some predictions wrong
* Confidence not perfectly aligned

Save:
saved_eval/logs/inference.log

---

## 🔶 4. STRUCTURED PREDICTIONS

Generate:

* predictions.json (full dataset)
* inference_samples.json (subset)

Include per-task predictions + probabilities

---

## 🔶 5. METRICS ENGINE OUTPUT

Generate:

Metrics:

* Accuracy
* Precision (macro/micro/weighted)
* Recall
* F1 (macro/micro/weighted)
* MCC
* ROC AUC
* Log Loss
* Balanced Accuracy
* Hamming Loss (emotion)
* Per-class F1

Also:

* worst_task_f1
* f1_imbalance_index

Save:
saved_eval/metrics/per_task_metrics.json
saved_eval/metrics/metrics_summary.json

---

## 🔶 6. CONFUSION MATRICES

For each task:

Requirements:

* Realistic counts
* Slight imbalance
* Logical misclassification patterns

Save:
saved_eval/matrices/confusion_<task>.csv
saved_eval/matrices/confusion_<task>.json

---

## 🔶 7. TASK CORRELATION MATRIX

Generate cross-task correlation heatmap values

Save:
saved_eval/metrics/task_correlation_matrix.csv

---

## 🔶 8. TRAINING CURVES

Generate arrays:

* epochs
* train_loss
* val_loss
* accuracy
* f1

Save:
saved_eval/curves/training_curves.json

---

## 🔶 9. ROC & CALIBRATION

Generate:

ROC:

* FPR
* TPR
* AUC

Calibration:

* Expected Calibration Error (ECE)
* Reliability bins

Save:
saved_eval/curves/roc_curves.json
saved_eval/curves/calibration_curves.json

---

## 🔶 10. ERROR ANALYSIS

Generate:

* Most confused classes
* Hard samples
* Misclassification patterns

Save:
saved_eval/reports/error_analysis.json

---

## 🔶 11. THRESHOLD OPTIMIZATION

Generate optimal thresholds per task

Save:
saved_eval/reports/thresholds.json

---

## 🔶 12. EXPLAINABILITY

Generate SHAP/LIME-like outputs:

* token importance
* positive/negative contributions

Save:
saved_eval/explanations/sample_*.json
saved_eval/explanations/sample_*.html

---

## 🔶 13. FULL EVALUATION REPORT

Match system structure:

Sections:

* Task Performance
* Overall Summary
* Calibration
* Error Analysis
* Thresholds
* Graph insights

Save:
saved_eval/reports/evaluation_report.json

---

## 🔶 14. SYSTEM LOGS

Generate logs:

* INFO (model loading, batches)
* DEBUG (tensor shapes, steps)
* WARNING (uncertainty, drift)

Save:
saved_eval/logs/system.log

---

## 🔶 15. AUDIT REPORT

Generate:

* system health
* drift signals
* reliability score
* risk flags

Save:
saved_eval/audit/system_audit.json

---

## 🔶 16. CONSISTENCY (CRITICAL)

ALL outputs MUST ALIGN:

* Metrics ↔ confusion matrices
* ROC ↔ AUC
* Logs ↔ training progression
* Error analysis ↔ confusion patterns

Add slight imperfections (1–3%) for realism.

---

## FINAL OUTPUT FORMAT

Return all files with file paths + content.

Everything must look like it came from a real ML pipeline used in production at scale.
